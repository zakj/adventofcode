[tool.pdm.venv]
prompt = "aoc"

[tool.pdm.scripts]
watch = "python -m aocli -w"

[project]
name = ""
version = ""
description = ""
authors = [{ name = "Zak Johnson", email = "me@zakj.net" }]
dependencies = [
    "mypy>=1.7.0",
    "numpy>=1.26.1",
    "rich>=13.6.0",
    "ruff>=0.1.13",
    "watchdog>=3.0.0",
    "websockets>=12.0",
]
requires-python = ">=3.12,<3.13"
license = { text = "MIT" }
